#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

// 仕様の固定

// MAP中の空行を許容するか禁止するかを決め、判定とテストを一致させる。

// MAP先頭のスペース（インデント）を許容するか、どこまで許容するかを明文化。

// ログ表記の統一

// J_INVALID / Invalid の揺れを消す（回帰テストが安定します）。

// 回帰テストの拡張（追加すべき失敗系）

// テクスチャ重複、F/C重複

// STARTが0個・複数

// MAP中に要素行が混入

// F/Cのフォーマット崩れ（区切り、桁、余計な文字）

// 閉路バリデーションで落ちる代表例（外部漏れ、穴）